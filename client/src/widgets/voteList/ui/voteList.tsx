import { type VoteDto } from "widgets/vote/model/types"

interface Props {
    vote: VoteDto
}

export const VoteList = ({
    vote
}: Props): JSX.Element => {

    return (
        <div>

        </div>
    )
}